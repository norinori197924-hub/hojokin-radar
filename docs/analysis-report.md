# 分析結果（トップページUI改善）

対象: `scripts/generate_site.py` / `templates/index.html` / `templates/_macros.html` /
`templates/base.html` / `static/filter.js` / `docs/style.css`

担当: analyzer サブエージェント

## 1. パフォーマンス

- 画像不使用・CSSはlink標準読み込み・`filter.js`は`defer`・GA4スクリプトは`async`
  → レンダーブロッキングなし（良好）
- **[要改善→修正済み]** ソート切替(`applySort`)が`list.appendChild()`を最大295回
  ループ実行しており、都度リフローが発生しうる構造だった
  → `DocumentFragment`でバッチ化してから一括appendするよう修正
- **[将来課題]** 全295件のカードを一度にDOM生成しており、日次自動データ追加で
  今後も件数が増加し続ける。表示件数の絞り込み・段階的読み込みの検討が将来的に
  望ましいが、今回のUI改善フェーズのスコープ（トップの検索〜一覧のコア体験）を
  超えるため、SPEC.md更新のうえ別途対応とする。

## 2. SEO

- title/meta description、h1→h2の見出し階層（今回`<h1>`をヒーロー検索見出しに
  新設し、既存の「h1が存在しない」課題を解消）、noscript時も本文が全件表示される
  設計（cloaking懸念なし）は良好
- **[将来課題]** og:image・og:url・canonicalタグの未設定、JSON-LD未実装は
  SEO観点で改善余地があるが、`templates/base.html`は詳細ページ(`detail.html`)
  にも影響する共有テンプレートであり、かつSPEC.mdのスコープ（トップページの
  検索・一覧UI）外のため、今回は変更しない。CLAUDE.mdにも「フェーズ2以降：
  JSON-LD、sitemap.xml等は未着手」と明記されており、次フェーズでの対応を推奨。

## 3. GA4計測

- GA4スニペットは`base.html`に1箇所のみでmeasurement ID二重挿入のリスクなし
- **[要改善→修正済み]** 目的キーワードチップのON/OFF操作で`trackFilterUse`に渡す
  値が常に同一（`isActive`による分岐が計測に反映されていなかった）ため、GA4上で
  「絞り込み適用」と「解除」が区別できない状態だった
  → `filter_action`（apply/clear）パラメータを追加し、区別できるよう修正
- **[要改善→修正済み]** 既に選択中のソートボタンを再クリックしても無条件で
  イベント送信され、同一操作が水増し計上される状態だった
  → 現在の選択と同じ場合は送信しないガードを追加
- **[将来課題]** GA4_ID未設定時にビルドは成功するが計測タグが無音で欠落する。
  CI側でのsecret存在チェック等の運用改善は本UI改善のスコープ外とする。

## 修正サマリ（本レポート作成後に適用）

- `static/filter.js`: ソートのDOM操作をDocumentFragmentでバッチ化
- `static/filter.js`: chipクリックのGA4イベントに`filter_action`(apply/clear)を追加
- `static/filter.js`: ソート再クリック時の重複イベント送信を抑止

## 対応しなかった項目とその理由

- OGP画像/canonical/JSON-LD追加、ページネーション導入、GA4_ID未設定時のCIチェック
  強化は、いずれも今回のSPEC.md（トップページ検索〜一覧のコア体験改善）の対象外、
  または`base.html`など詳細ページにも影響する共有部分の変更となるため、
  ユーザー承認済みのスコープを超えると判断し、今回は変更していない。
  対応する場合はSPEC.mdの更新・承認を先に行う。

---

# 分析レポート（広告枠プレースホルダー追加）

担当: analyzer サブエージェント（パフォーマンス・SEO・GA4観点）

## パフォーマンス

| 指摘 | 重大度 | 対応 |
|---|---|---|
| 現時点(プレースホルダーのみ)ではCLS・追加リクエストなし、min-height:100pxで最低限確保済み | -（問題なし） | 対応不要 |
| min-height:100pxは「ゼロ回避」の暫定値。実際のAdSense広告フォーマット(150〜300px超になりうる)確定時に差し替えないとCLSが発生しうる | 高 | 実広告差し込み時に対応（今回は設計として明記） |
| 広告枠自体はJS不要な静的HTML/CSSのみ、render-blockingリソース増加なし | -（問題なし） | 対応不要 |

## SEO

| 指摘 | 重大度 | 対応 |
|---|---|---|
| メタタグ・見出し構造(h1/h2)への影響なし | -（問題なし） | 対応不要 |
| `ad-slot-index-top`がページ本文冒頭(h2「締切間近」より前)に配置されており、実広告差し込み後にAds Above the Foldガイドライン(ファーストビュー過度占有)に抵触しないか要確認 | 中 | 実広告差し込み時に確認（今回はプレースホルダーの見た目上問題なし） |
| index.htmlにh1が存在しない（既存の問題、今回のスコープ外） | -（既存課題） | 対応不要 |
| detail.htmlの`ad-slot-detail-top`はパンくず直後・本文(h1・table)より前だが、パンくずはナビゲーション要素でありSEO上の重大な問題はない | -（問題なし） | 対応不要 |

## 計測タグ

| 指摘 | 重大度 | 対応 |
|---|---|---|
| GA4スクリプト(`<head>`内)と広告枠(`<body>`内)は完全に独立、重複・欠落なし | -（問題なし） | 対応不要 |
| 広告インプレッション/クリック計測のフックが現状ない(スコープ通り) | 低 | 今回は見送り（将来AdSense導入時に`id`属性を流用してGA4連携を検討） |

## 今回対応する項目

なし（高重大度の指摘は「実際の広告フォーマット確定時に再設定する」性質のもので、
プレースホルダー段階の今回は対応不要と判断。design.mdに申し送り事項として明記する）

## 今回スコープ外（将来の申し送り事項）

1. 実広告タグ差し込み時に、実際のフォーマットに合わせて`.ad-slot`の`min-height`
   （必要ならレスポンシブ対応も）を再設定する
2. `ad-slot-index-top`のファーストビュー占有について、実広告差し込み後に
   Ads Above the Foldガイドラインの観点で再確認する
3. 広告インプレッション/クリックのGA4計測を導入する場合、`id`属性
   （`ad-slot-xxx`）をイベントlabelとして流用する設計にできる

---

# 分析レポート（SPEC.md セクション9 v1.2追加改善）

担当: analyzer サブエージェント（パフォーマンス・SEO・GA4観点）

## パフォーマンス

| 指摘 | 重大度 | 対応 |
|---|---|---|
| 追加CSS/JS/HTMLは既存stylesheetへの追記・既存`applyFilters()`ループへの相乗りのみで、新規HTTPリクエストや全カード再走査は発生しない | -（問題なし） | 対応不要 |
| `.hero-lead`・`.filter-feedback`はページ全体で1箇所ずつのみ追加（カード単位の繰り返し要素ではない） | -（問題なし） | 対応不要 |

## SEO

| 指摘 | 重大度 | 対応 | 
|---|---|---|
| meta description / og:descriptionへの採用文面(66字)の反映、h1→h2の見出し階層は壊れていない | -（問題なし） | 対応不要 |
| **[要改善→修正済み]** meta description用文言(block description)と画面表示用`.hero-lead`が別々にハードコードされ、将来の文言変更時に修正漏れで食い違うリスクがあった | 中 | `templates/index.html`で`{% set hero_lead_text %}`に一元化し、両方から参照するよう修正 |
| meta description文字数(66字)は日本語検索結果表示目安(70〜100字程度)よりやや短いが、確定文面（SPEC.md 9-1）のため今回は変更しない | 低（将来課題） | 対応不要（文言はユーザー確定事項） |
| JSON-LD等の構造化データは未実装だが、CLAUDE.md記載どおりフェーズ2スコープ | -（既存課題） | 対応不要 |

## 計測タグ

| 指摘 | 重大度 | 対応 |
|---|---|---|
| クリアボタン操作で`trackFilterUse("clear", "all")`が正しく発火し、既存のarea/industry/keyword/sort/keyword_chipイベントとは独立（二重発火なし） | -（問題なし） | 対応不要 |
| GA4スニペット自体（`base.html`）は今回変更なし、二重挿入等のリスクなし | -（問題なし） | 対応不要 |

## 今回対応した項目

- `templates/index.html`: meta description用文言と`.hero-lead`表示文言を`{% set hero_lead_text %}`に一元化（修正漏れリスクの解消）

## 今回スコープ外（将来の申し送り事項）

- meta descriptionの文字数最適化（70〜100字目安への微調整）は、文言自体がSPEC.md 9-1で確定済みのため今回のフェーズでは対象外。次回文言見直し時に検討。

---

# 分析レポート（SPEC.md セクション10 v1.3追加改善）

担当: analyzer サブエージェント（パフォーマンス・SEO・GA4観点）

## パフォーマンス

| 指摘 | 重大度 | 対応 |
|---|---|---|
| `.site-tagline`削除はheader内p要素1つ・CSSルール1個分の軽量化で、リクエスト数やレンダリングブロックに影響しない | -（問題なし） | 対応不要 |
| `.card`ホバーの`transform`/`box-shadow`はhover時のみのローカル処理で、298件のカード初期描画コストには影響しない。画像アセット追加なしのCSSグラデーションも同様に軽量 | -（問題なし） | 対応不要 |
| `docs/filter.js`に`ad-slot`関連のセレクタ・処理は存在せず、広告枠の位置変更はJS実行パスに影響しない（JS自体は今回無変更） | -（問題なし） | 対応不要 |

## SEO

| 指摘 | 重大度 | 対応 |
|---|---|---|
| meta description / h1はv1.2で設定済みの`hero_lead_text`をそのまま参照しており、削除した`.site-tagline`の文言とは独立。削除の影響を受けていない | -（問題なし） | 対応不要 |
| 見出し階層（index.html: h1×1・h2×1、detail.html: h1×1）に変更なし | -（問題なし） | 対応不要 |
| header内のテキストが1行減るが、同内容はhero-search内のh1/リード文に実質的に内包されており、重複解消に近い。SEO上意味を持つtitle/meta description/見出し/本文はv1.2のまま不変 | -（問題なし） | 対応不要 |

## 計測タグ

| 指摘 | 重大度 | 対応 |
|---|---|---|
| GA4スニペット（`templates/base.html`）は今回変更なし、二重挿入等のリスクなし | -（問題なし） | 対応不要 |
| 広告枠の位置変更（`ad-slot-index-list`削除・`ad-slot-index-bottom`新設）は`#all-items-list`の子要素構成（カード＋`#filter-no-match`）に影響せず、`trackFilterUse`等の既存イベント計測ロジックとは無関係 | -（問題なし） | 対応不要 |

## 今回対応した項目

なし（全項目「良好」。SPEC.md 10-1〜10-3の完了条件を満たし、既存のSEO設定・GA4計測・フィルタ/ソートロジックへの副作用は確認されなかった）

## 今回スコープ外（将来の申し送り事項）

- JSON-LD構造化データ・OGP画像（og:image）の未実装は既存課題（CLAUDE.md記載どおりフェーズ2スコープ）であり、v1.3差分には含まれないため今回は対象外。
