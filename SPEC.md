# SPEC.md — hojokin-radar UI改善（フェーズ1: コア体験）

- **対象プロジェクト**: hojokin-radar（補助金レーダー）
- **バージョン**: v1.3（公開後レビュー第2回の改善3点を追記）
- **作成日**: 2026-07-13（v1.2追記: 2026-07-14 / v1.3追記: 2026-07-14）
- **ステータス**: ✅ v1.2まで実装完了 ＋ v1.3差分の確認待ち

---

## 1. 概要と目的

補助金を探すユーザーが「自分に使える補助金を素早く見つけられる」ことを最優先に、トップページの検索・一覧まわりのUIを刷新する。現状は検索窓の位置・絞り込み軸・一覧の見やすさに課題があり、「調べやすさ」に改善余地がある。今回はサイト全体ではなく **コア体験（トップの検索〜一覧）** に集中する。

---

## 2. ターゲットユーザーと主要な導線

補助金を探している事業者・個人。想定される検索の入口を、ユーザーヒアリングに基づき以下の優先順位で設計する。

| 優先 | 入口 | UIでの扱い |
|------|------|-----------|
| 1 | 地域（都道府県） | 主軸。最も目立たせる |
| 2 | 目的・キーワード | 主軸。チップで直感的に選べるように |
| 3 | 業種 | 絞り込み軸 |
| 4 | 新着 | ソート（並び替え） |
| 5 | 締切が近い順 | ソート（並び替え） |

**主要導線**: トップページ着地 → 検索窓（最上部）で地域・目的・業種を指定 → 一覧をソート（締切/新着）→ 気になる補助金の詳細へ。

---

## 3. ページ構成・サイトマップ

今回の変更対象は **トップページのみ**。

- **トップページ（改修対象）**
  - ファーストビュー: 検索窓（最上部・大きく）
  - 地域選択（47都道府県）
  - 目的・キーワードチップ
  - 業種絞り込み
  - ソート切替（締切が近い順 / 新着順）
  - 補助金一覧（カード表示）
- 詳細ページ（今回は改修対象外・フェーズ2）

---

## 4. デザイン方針

### トーン
- 信頼感・見やすさ重視。行政/補助金という性質上、誠実で読みやすい印象。
- 情報が探しやすいことを最優先し、装飾より可読性・操作性を優先。

### レイアウト方針
- **検索窓を最上部（ファーストビュー最上部）に大きく配置**。補助金探しが主目的のため。
- **地域（都道府県）を一番目立つ位置に**。47都道府県から選択可能にする。
- **目的・キーワードはチップ形式**。前面によく使う目的を8〜10個ほど表示し、残りは「もっと見る」で展開。
  - ※チップに出す目的カテゴリはおすすめ構成（一般的に検索されやすい目的を想定）。実装時に実データの21キーワードと突き合わせて調整する。
- **業種は絞り込み軸**として、地域・目的の次に配置。
- **新着 / 締切が近い順はソート**として一覧上部に配置。
- **一覧はカード表示**。情報の優先順位を明確に（補助金名・締切・地域・金額を目立たせる）。
- **各カードに2〜3行の要約を常時表示**（どんな補助金か・対象・特徴が一覧上で分かるように）。要約により、詳細ページに飛ばずとも「自分向きか」を一覧で判断できるようにする。
  - ※要約データの扱い：既存データの `summary`（または `catch_phrase`）フィールドを2〜3行に整形して表示する。
  - ※**既存CLAUDE.mdの設計原則「推測でデータを補完しない」に従い、summaryが空／不明の場合はAIによる新規要約生成を行わず、要約欄を非表示にするか「詳細ページでご確認ください」等の定型文で対応する。** データ取得・要約生成ロジックの新設は本フェーズのスコープ外。
- **カードクリック（または「詳細を見る」）で既存の詳細ページへ遷移**（従来型の遷移方式）。詳細ページ自体の改修は今回のスコープ外（遷移先として利用するのみ）。

### 配色・参考URL
- 配色は既存サイトのトーンを踏襲しつつ、可読性を高める方向で調整。
- 参考URL: （未指定。モックアップ確認時にすり合わせ）

---

## 5. 技術スタックと制約

- 静的サイト（GitHub Pages ホスティング）
- HTML / CSS / JavaScript（既存構成を踏襲）
- 既存資産: 検索・フィルタ機能（都道府県別・業種別・キーワード）は実装済み。今回はその上に締切/新着ソートを追加し、UI配置・見せ方を刷新する。
- Node.js未導入環境のため、ビルドツールに依存しない構成とする。
- ブラウザツール（claude-in-chrome）は使用しない。
- 実装は Claude Code の `/build-site` ワークフローで行う。
- プロジェクトルートの既存 `CLAUDE.md`（技術仕様・設計原則）を踏襲する。特に「一次情報リンク必須」「免責表示」「締切切れの自動除外」「推測でデータを補完しない」の4原則は、今回のUI改善でも遵守する。

---

## 6. スコープ外のこと（今回やらないこと）

- 詳細ページのデザイン改修 → フェーズ2
- **市区町村単位の検索（例: 東京都世田谷区）** → フェーズ2の将来課題。データが市区町村レベルの情報を持つか確認が必要。今回は47都道府県までとする。
- プライバシーポリシー・運営者情報ページの新設 → 別タスク（審査準備フェーズ）
- 実際の広告タグ（AdSense/ASP）の挿入 → 審査・登録後に別途対応
- データ取得ロジック・スクレイピングの変更

---

## 7. 完了条件（検証可能な形で）

- [ ] トップページ最上部に検索窓が大きく配置されている
- [ ] 47都道府県から地域を選択して絞り込める
- [ ] 目的・キーワードをチップ形式で選択でき、「もっと見る」で全キーワードが展開できる
- [ ] 業種で絞り込める
- [ ] 「締切が近い順」で一覧をソートできる
- [ ] 「新着順」で一覧をソートできる
- [ ] 一覧カードに補助金名・締切・地域・金額が視認しやすく表示されている
- [ ] 各カードに2〜3行の要約が表示されている
- [ ] カードクリック（または「詳細を見る」）で該当補助金の詳細ページに遷移できる
- [ ] スマホ表示（375px幅程度）でレイアウトが崩れない
- [ ] 既存の検索・フィルタ結果が正しく機能している（デグレがない）

---

## 8. 補足メモ

- Aの目的キーワードチップは、実装時に実際の21キーワードと突き合わせて前面表示分を最終確定する。
- GitHub Pages はデイリー処理のワークフロー実行時に更新される点に留意（コードpushだけでは反映されない場合がある）。

---

## 9. v1.2 追加改善（公開後レビュー 2026-07-14）

フェーズ1実装の公開後、実際の画面を確認したうえでの改善5点。いずれもトップページの範囲内であり、フェーズ1の延長として扱う。

### 9-1. ヒーロー部のサイト説明文の充実
現状の1行説明を、サイトの価値が伝わる2〜3行のリード文に変更する。

採用文面（確定）:
> 全国の補助金・助成金情報を毎日自動収集し、締切順に整理。地域・目的・業種からかんたんに検索できます。すべて公式ページへのリンク付き。

- サイト名「補助金レーダー」は変更しない。
- meta description にも同趣旨の文面を反映する（SEO対策）。

### 9-2. 絞り込み状態のフィードバック表示
チップ・地域・業種・キーワードで絞り込んだ際、一覧の直上に現在の絞り込み条件と件数を表示する。

例: 「『人材』で絞り込み中：34件」「東京都 × IT導入：12件」

- 絞り込みなしの場合は「全299件を表示中」等の全件表示とする(件数は実データに従う)。
- 表示は絞り込み操作のたびに即時更新する。

### 9-3. 絞り込みクリアボタン
「絞り込みをクリア」(すべて表示に戻す)ボタンを設置する。

- 配置は 9-2 のフィードバック表示の隣を想定。
- クリックで地域・目的チップ・業種・キーワードのすべてをリセットし、全件表示に戻す。
- 絞り込みが何もない状態では非表示または非活性とする。

### 9-4. 目的チップの語彙改善
頻度順で選定した結果、補助金サイトでは自明で絞り込みに寄与しない汎用語(「事業」「補助」「支援」「助成」「中小企業」等)がチップに含まれている。これらを除外リストで前面表示から外し、意味のある目的語(「脱炭素」「事業承継」「ものづくり」「人材」等)を優先する。

- 除外リストは generate_site.py 側に定義し、頻度順ロジック自体は維持する。
- 除外した語も「もっと見る」内には表示してよい(検索自体は可能なままとする)。
- ※既存CLAUDE.mdの原則どおり、チップはデータに実在するキーワードのみで構成し、新規語を創作しない。

### 9-5. 完了条件の追加(v1.2 分)
- [ ] ヒーロー部に採用文面のリード文が表示されている
- [ ] 絞り込み操作時、一覧直上に条件と件数が表示され、操作のたびに更新される
- [ ] 「絞り込みをクリア」で全条件がリセットされ全件表示に戻る
- [ ] 前面チップに汎用語(事業・補助・支援・助成・中小企業 等)が含まれていない
- [ ] 上記変更後も既存の検索・フィルタ・ソートが正しく機能している(デグレなし)

---

## 10. v1.3 追加改善(公開後レビュー第2回 2026-07-14)

v1.2公開後の実機確認に基づく改善3点。トップページの範囲内であり、UIレイアウト構造は変更しない。

### 10-1. ヒーロー部の文言を3段構成に再構成
現状の「中小企業・個人事業主向けの補助金・助成金を締切順に自動収集」の行は削除し、以下の3段構成とする。

1. サイト名: 補助金レーダー
2. キャッチコピー: あなたに使える補助金を見つける
3. リード文: 全国の補助金・助成金情報を毎日自動収集し、締切順に整理。地域・目的・業種からかんたんに検索できます。すべて公式ページへのリンク付き。

- 3つの文言はサイト名直下にまとまりとして表示し、視覚的な階層(サイズ・太さ)を付ける。
- 検索ボックス群はその下に続く。meta description等は変更しない(v1.2で設定済み)。

### 10-2. 一覧見出し直下の広告枠を削除
「補助金・助成金一覧 全N件」の見出しと最初のカードの間にある広告枠は、絞り込み結果への視線を分断するため削除する。

- 広告枠は次の2箇所に限定する: (a) 検索エリアの下、(b) 一覧の途中(5件目の後を目安)または一覧最下部。
- 広告枠はプレースホルダーのままとし、実タグの挿入は引き続きスコープ外。

### 10-3. デザイン改善(案A: ブルー基調のまま深みを足す)
UIレイアウト・要素の配置は変更せず、以下の視覚的な質感向上のみ行う。

- ヒーロー部の背景に、ごく薄いブルー系のグラデーションを追加(可読性を損なわない範囲)。
- 一覧カードにホバー時の浮き上がり効果(影の強調・わずかな持ち上がり)を追加。
- 締切バッジ(残り日数)・NEWバッジの配色を現状より鮮やかにし、視認性を高める。
- 配色はブルー基調を維持し、行政・補助金情報サイトとしての信頼感を損なわないこと。
- 過度なアニメーションは追加しない(ホバー時のtransition程度に留める)。

### 10-4. スコープ外(v1.3で変更しないこと)
- 目的チップの選択/解除の挙動・見た目(現状のまま維持)
- UIレイアウト構造・フォントファミリー
- 詳細ページ・データ取得ロジック・実広告タグ・市区町村検索(従来どおり)

### 10-5. 完了条件(v1.3分)
- [ ] ヒーロー部がサイト名→キャッチコピー→リード文の3段構成になっており、旧説明文が削除されている
- [ ] 一覧見出しと最初のカードの間に広告枠がない
- [ ] 広告枠が「検索エリア下」「一覧の途中または最下部」の2箇所のみになっている
- [ ] ヒーロー背景のグラデーション、カードのホバー効果、バッジの配色強化が反映されている
- [ ] チップの挙動・見た目が変わっていない
- [ ] 既存の検索・フィルタ・ソート・フィードバック表示・クリアボタンが正しく機能している(デグレなし)
